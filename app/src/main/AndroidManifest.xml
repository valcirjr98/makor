<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.valcirjr98.makor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.CadastroActivity" />
        <activity
            android:name=".view.MenuPrincipalActivity"
            android:label="@string/title_activity_menu_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.AdicionaNovoPedidoActivity" />
        <activity android:name=".view.AdicionaNovoProdutoActivity" />
        <activity android:name=".view.AdicionaNovoClienteActivity" />
        <activity android:name=".Calendario" />
        <activity android:name=".MeusProdutos" />
        <activity
            android:name=".MenuPrincipal3"
            android:label="@string/title_activity_menu_principal3"></activity>
    </application>

</manifest>